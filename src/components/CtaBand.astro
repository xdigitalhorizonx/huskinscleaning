---
import Icon from "./Icon.astro";
import { site } from "../data/site";
interface Props {
  heading?: string;
  text?: string;
}
const {
  heading = "Ready to find your horizon?",
  text = "Free 30-minute discovery call. We'll talk about what you're trying to grow, what's getting in the way, and whether we're the right team to help — no pitch deck.",
} = Astro.props;
---

<section class="section cta-band scanlines">
  <div class="grid-floor"></div>
  <div class="container">
    <div class="cta-panel panel is-red">
      <div class="cta-copy">
        <span class="eyebrow is-red">Let's talk</span>
        <h2 class="h2 display mt-1">{heading}</h2>
        <p class="lead mt-2">{text}</p>
      </div>
      <div class="cta-actions">
        <a href="/contact/" class="btn btn-primary btn-lg"><Icon name="rocket" size={18} /> Start a Project</a>
        <a href={site.phone.href} class="btn btn-ghost btn-lg"><Icon name="phone" size={18} /> Call {site.phone.display}</a>
        <p class="cta-note mono"><span class="status-dot"></span> {site.tagline.toUpperCase()}</p>
      </div>
    </div>
  </div>
</section>

<style>
  .cta-band {
    position: relative;
    overflow: hidden;
    border-block: 1px solid var(--line);
    background:
      radial-gradient(600px 300px at 80% 100%, rgba(34, 224, 255, 0.08), transparent 60%),
      var(--void-2);
  }
  .cta-panel {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    gap: clamp(1.5rem, 4vw, 3rem);
    align-items: center;
    position: relative;
    z-index: 3;
    background:
      linear-gradient(180deg, rgba(255, 90, 60, 0.06), transparent 50%),
      var(--surface);
  }
  .cta-copy .lead { max-width: 46ch; }
  .cta-actions { display: grid; gap: 0.8rem; }
  .cta-note {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.68rem;
    letter-spacing: 0.1em;
    color: var(--muted);
    margin-top: 0.3rem;
  }
  @media (max-width: 820px) {
    .cta-panel { grid-template-columns: 1fr; }
  }
</style>
